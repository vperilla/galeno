<xml version="1.0" encoding="utf-8" xmlns:py="http://genshi.edgewall.org/">
<tryton>
    <data skiptest="1" noupdate="1">
        <py:for each="contraceptive in contraceptives">
        <record model="galeno.contraceptive" id="contraceptive_${contraceptive['number']}">
            <field name="code">${contraceptive['gender']}</field>
            <field name="name">${contraceptive['name']}</field>
            <field name="description">${contraceptive['description']}</field>
        </record>
        </py:for>
    </data>
</tryton>
</xml>
